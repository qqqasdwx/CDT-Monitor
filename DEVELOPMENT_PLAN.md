# DEVELOPMENT_PLAN.md

## 项目开发规划

本项目目标是构建一个轻量级自托管控制台，用于监控阿里云 CDT 流量、管理 ECS 生命周期，并在接近流量额度或费用风险线时执行保护动作。

开发策略：

- 先完成最小业务闭环，再逐步产品化。
- 保持单体、轻量、清晰，不引入复杂架构。
- 前端保持薄层，后端负责业务逻辑、权限、校验和持久化。
- `reference/cdt.sh` 作为核心业务原型参考：查询流量、比较阈值、启动或停止 ECS。
- `reference/CDT-Monitor` 和 `reference/ecs-controller` 作为功能范围和领域流程参考，不照搬 PHP 架构。

## 阶段 0：项目骨架

目标：建立可运行、可扩展但不过度设计的前后端基础结构。

实现内容：

- 创建 `backend/` Go 服务。
- 创建 `frontend/` React + Vite + TypeScript 应用。
- 接入 Tailwind CSS 和 shadcn/ui。
- 后端接入 SQLite 和基础 migration 机制。
- 建立基础 HTTP API、统一 JSON 响应和统一错误格式。
- 建立前端 API client，不在组件中散落 `fetch`。
- 提供本地开发启动方式。

建议交付物：

- `backend/cmd/server/` 后端入口。
- `backend/internal/config/` 配置加载。
- `backend/internal/http/` 路由和健康检查。
- `backend/internal/store/` SQLite 连接和 migration。
- `frontend/src/api/` API client。
- `frontend/src/components/` 基础 UI 组件使用方式。

验收标准：

- 后端服务能启动。
- 前端开发服务能启动。
- 前端能调用后端健康检查接口。
- SQLite 空库能初始化。
- `go test ./...` 和前端构建命令可运行。

## 阶段 1：核心 MVP

目标：把 `reference/cdt.sh` 的最小闭环产品化。

核心闭环：

1. 用户配置阿里云账号、区域、实例 ID 和流量额度。
2. 后端定时同步 CDT 流量和 ECS 实例状态。
3. 后端根据阈值判断是否需要保护。
4. 达到保护条件时，后端自动停止 ECS。
5. 系统记录动作日志，前端展示状态和结果。

实现内容：

- 账号配置：AccessKey ID、AccessKey Secret、区域、备注。
- 实例配置：实例 ID、流量额度、启用状态。
- 流量同步：调用阿里云 CDT API 查询当前流量。
- 状态同步：调用 ECS API 查询实例状态。
- 阈值熔断：按配置阈值自动停机。
- 停机模式：支持普通停机和节省停机。
- 手动操作：启动实例、停止实例。
- 审计日志：同步结果、阈值判断、自动停机、手动操作、错误信息。
- 前端页面：实例列表、流量使用、阈值、实例状态、最近同步时间、最近动作。

关键规则：

- 后端负责所有阈值判断和实例操作。
- 前端只提交配置和操作请求，不自行判断是否应该停机。
- 自动停机后要记录原因，避免后续保活逻辑误启动。
- API 失败、鉴权失败、实例不存在等错误必须明确记录。

验收标准：

- 能配置一个账号和一个实例。
- 能同步 CDT 流量和 ECS 状态。
- 超过阈值后能触发自动停机。
- 前端能展示当前状态和最近日志。
- 手动启动/停止走后端 API，并记录日志。

## 阶段 2：事件监听和秒级保活

目标：使用阿里云云监控事件订阅 + webhook，实现实例停止后的秒级保活。

背景：

分钟级巡检能实现保活，但响应速度有限。阿里云云监控支持系统事件订阅，可订阅 ECS 实例状态改变通知，并通过 webhook 推送到本项目。后端收到 `Stopped` 事件后，可以立即判断是否需要重新启动实例。

阿里云订阅建议：

- 事件类型：系统事件。
- 产品：云服务器 ECS。
- 事件类型：状态通知。
- 事件名称：实例状态改变通知。
- 事件内容：目标实例 ID。
- 通知方式：webhook。

实现内容：

- 后端提供 webhook endpoint，例如 `POST /api/webhooks/aliyun/events/:token`。
- 为每个系统生成 webhook token，用于防止公开接口被随意调用。
- 保存 webhook 原始事件和解析后的事件字段。
- 根据事件 ID、实例 ID、事件时间做幂等去重。
- 收到 `Stopped` 事件后，调用 ECS API 二次确认实例真实状态。
- 判断是否允许保活启动。
- 符合条件时调用 StartInstance。
- 记录事件日志、动作日志和通知。

保活启动条件：

- 实例启用了保活。
- 实例当前确认为 `Stopped`。
- 不是系统因流量超限主动停机。
- 不是用户手动停机后设置的阻止自动启动状态。
- 实例没有处于释放、停机中、启动中等过渡状态。
- 没有命中冷却时间。

必须避免的行为：

- 用户手动停机后被 webhook 自动拉起。
- 流量超限停机后被 webhook 自动拉起。
- webhook 重试导致重复启动。
- 未校验 token 的 webhook 直接触发云资源操作。
- 不确认云端状态就直接执行启动。

兜底机制：

- 保留低频巡检，例如每 5-10 分钟一次。
- 巡检用于处理 webhook 丢失、事件延迟、API 状态不一致和本地状态校准。
- CDT 流量同步仍依赖周期任务，事件监听不能替代流量巡检。

推荐架构：

```text
周期任务：流量同步、阈值熔断、状态校准
事件监听：实例 stopped 后秒级保活
手动操作：用户主动 start/stop
```

验收标准：

- 后端能接收并记录阿里云事件 webhook。
- 重复事件不会重复触发启动。
- `Stopped` 事件能在保活允许时触发启动。
- 流量超限停机不会被保活拉起。
- 用户手动停机不会被保活拉起。
- webhook 失败或丢失时，低频巡检仍能校准状态。

## 阶段 3：配置和通知系统

目标：完善系统设置和通知能力，让关键动作可被用户及时感知。

实现内容：

- 系统设置页：阈值、停机模式、同步频率、保活开关、冷却时间。
- 通知通道：
  - 邮件
  - Telegram
  - 通用 webhook
- 通知事件：
  - 流量接近阈值
  - 流量超限自动停机
  - webhook 收到实例停止事件
  - 保活启动成功
  - 保活启动失败
  - 阿里云 API 鉴权失败
  - 定时任务执行失败
- 通知测试功能。
- 通知发送结果记录到日志。

关键规则：

- 通知由后端统一发送。
- 前端只负责配置和测试入口。
- 通知失败不能伪装成功。
- 通知中不能包含 AccessKey Secret、Token、密码等敏感信息。

验收标准：

- 用户能配置至少一种通知方式。
- 能发送测试通知。
- 自动停机和保活动作能触发通知。
- 通知失败有明确错误日志。

## 阶段 4：历史、图表和审计

目标：提升可观测性，方便用户理解流量趋势和排查问题。

实现内容：

- 流量历史记录：小时维度、天维度。
- 实例状态历史。
- 动作日志：启动、停止、自动停机、保活启动、手动操作。
- 事件日志：webhook 原始事件、解析结果、处理结果。
- 前端图表：流量趋势、阈值线、最近操作。
- 日志清理策略，避免 SQLite 无限增长。

关键规则：

- 日志结构应利于排查，不只保存一段不可检索文本。
- 原始 webhook 事件可保存必要字段，不保存无用大对象。
- 图表数据由后端聚合，前端只负责展示。

验收标准：

- 能查看最近流量趋势。
- 能查看实例状态变化历史。
- 能区分动作日志和事件日志。
- 日志可以按时间或类型清理。

## 阶段 5：增强功能

目标：在核心稳定后，逐步补齐高级能力。

候选功能：

- 多账号、多区域、多实例管理。
- 定时开机、定时停机。
- 月初自动恢复。
- 抢占式实例保活冷却策略。
- 费用和余额查询。
- Cloudflare DDNS。
- ECS 一键创建。
- ECS 释放。
- 更换公网 IP。
- Telegram 远程控制。

优先级建议：

1. 多账号、多实例管理。
2. 定时开机/停机。
3. 月初自动恢复。
4. 费用和余额查询。
5. DDNS。
6. ECS 创建、释放、换 IP。
7. Telegram 远程控制。

关键规则：

- 高风险云资源操作必须放在后端。
- 释放实例、换 IP、改 DNS 等操作必须有明确确认、日志和失败处理。
- 不为多云提前抽象 provider 层。
- 不一次性重做 `reference/ecs-controller` 的全部功能。

验收标准：

- 每个增强功能都应独立闭环。
- 每个高风险动作都可审计。
- 功能增加后不破坏阶段 1 和阶段 2 的核心保护能力。

## 长期原则

- CDT 流量保护依赖周期同步。
- ECS 异常停机恢复优先依赖云监控事件 webhook。
- 低频巡检永远保留，用于兜底和校准。
- 前端不承载业务规则。
- 后端不吞错误。
- SQLite 保持简单可迁移。
- 先做可靠，再做漂亮，再做高级功能。
