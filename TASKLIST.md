# TASKLIST.md

## 使用说明

本文件把 `DEVELOPMENT_PLAN.md` 拆成可执行任务。开发时按阶段推进，每个任务应保持小步提交、可验证、可回滚。

任务状态约定：

- `[ ]` 未开始
- `[x]` 已完成

阶段推进规则：

- 阶段 0 必须先完成，避免在空仓库上直接堆业务代码。
- 阶段 1 是核心 MVP，优先级最高。
- 阶段 2 的事件监听不能替代阶段 1 的周期巡检。
- 阶段 3 之后的功能必须建立在阶段 1 和阶段 2 稳定的基础上。

## 阶段 0：项目骨架

目标：建立可运行、可测试、可继续扩展的轻量全栈基础结构。

### 0.1 仓库基础

- [ ] 确认 `reference/` 已加入 `.gitignore`，避免参考代码进入正式提交。
- [ ] 保留 `AGENTS.md`、`DEVELOPMENT_PLAN.md`、`TASKLIST.md` 作为项目协作和规划文档。
- [ ] 清理无业务价值的占位代码和临时 Docker 构建配置。
- [ ] 明确本地开发命令，避免依赖隐式环境。

### 0.2 后端基础

- [ ] 创建 `backend/` Go module。
- [ ] 创建 `backend/cmd/server/` 启动入口。
- [ ] 创建 `backend/internal/config/`，集中读取端口、数据库路径、日志级别等配置。
- [ ] 创建 `backend/internal/http/`，提供路由、handler、统一 JSON 响应和错误响应。
- [ ] 创建 `GET /healthz` 健康检查接口。
- [ ] 创建 `GET /api/v1/status` 基础状态接口。
- [ ] 建立结构化日志，不输出密钥、Token、密码等敏感信息。
- [ ] 所有请求链路使用 `context.Context`。

### 0.3 SQLite 基础

- [ ] 创建 `backend/internal/store/`。
- [ ] 接入 SQLite 连接管理。
- [ ] 启用 `PRAGMA foreign_keys = ON`。
- [ ] 创建简单 migration 机制。
- [ ] 创建首个空库初始化流程。
- [ ] 为 migration 增加执行记录表。
- [ ] 验证空库启动时能自动完成初始化。

### 0.4 前端基础

- [ ] 创建 `frontend/` React + Vite + TypeScript 项目。
- [ ] 开启 TypeScript strict 风格。
- [ ] 接入 Tailwind CSS。
- [ ] 接入 shadcn/ui。
- [ ] 建立 `frontend/src/api/` API client。
- [ ] 建立 `frontend/src/components/` 通用组件目录。
- [ ] 建立 `frontend/src/features/` 或 `frontend/src/pages/` 业务页面目录。
- [ ] 前端能调用后端 `GET /healthz` 并展示基础状态。

### 0.5 本地开发和验证

- [ ] 提供本地后端启动命令。
- [ ] 提供本地前端启动命令。
- [ ] 提供本地数据库目录配置。
- [ ] 确认 `go test ./...` 可运行。
- [ ] 确认前端 typecheck/build 命令可运行。
- [ ] 补充最小 README，说明开发启动方式。

## 阶段 1：核心 MVP

目标：把 CDT 流量保护的最小业务闭环产品化。

### 1.1 数据模型和迁移

- [ ] 设计 `aliyun_accounts` 表：AccessKey ID、加密后的 AccessKey Secret、区域、备注、启用状态、时间字段。
- [ ] 设计 `ecs_instances` 表：账号 ID、实例 ID、名称、流量阈值、停机模式、启用状态、保活配置、最近状态、时间字段。
- [ ] 设计 `traffic_snapshots` 表：账号 ID、统计周期、总流量、采集时间。
- [ ] 设计 `action_logs` 表：动作类型、触发来源、目标实例、结果、错误信息、时间字段。
- [ ] 为账号、实例、日志和采集时间添加必要索引。
- [ ] 写入 migration，并验证空库初始化。

### 1.2 阿里云客户端

- [ ] 在后端封装阿里云 CDT 查询客户端。
- [ ] 在后端封装阿里云 ECS 查询客户端。
- [ ] 在后端封装 ECS 启动操作。
- [ ] 在后端封装 ECS 停止操作，支持 `StopCharging` 和 `KeepCharging`。
- [ ] 为阿里云 API 错误建立明确错误类型或错误码映射。
- [ ] 确保日志和错误响应不泄露 AccessKey Secret。

### 1.3 配置管理 API

- [ ] 实现账号创建 API。
- [ ] 实现账号更新 API。
- [ ] 实现账号列表 API。
- [ ] 实现账号禁用或删除 API。
- [ ] 实现实例创建 API。
- [ ] 实现实例更新 API。
- [ ] 实现实例列表 API。
- [ ] 实现实例启用、禁用 API。
- [ ] 后端对 AccessKey、区域、实例 ID、阈值、停机模式做最终校验。

### 1.4 流量和实例状态同步

- [ ] 实现周期任务框架，支持可配置同步间隔。
- [ ] 实现 CDT 流量同步任务。
- [ ] 实现 ECS 实例状态同步任务。
- [ ] 保存最近同步时间、同步结果和错误信息。
- [ ] API 失败时记录日志，不伪造成功。
- [ ] 增加手动同步接口，便于前端触发和排查。

### 1.5 阈值保护

- [ ] 实现阈值判断 service。
- [ ] 当 CDT 流量达到阈值时，自动停止启用保护的 ECS 实例。
- [ ] 自动停机时记录触发原因、当时流量、阈值、停机模式和执行结果。
- [ ] 流量超限自动停机后，设置状态标记，避免后续保活逻辑误启动。
- [ ] 对停机中、已停止、启动中等状态做明确处理。
- [ ] 增加 dry-run 或测试模式，便于验证判断逻辑而不操作真实 ECS。

### 1.6 手动操作

- [ ] 实现手动启动 ECS API。
- [ ] 实现手动停止 ECS API。
- [ ] 手动停止时记录用户意图，避免保活逻辑自动拉起。
- [ ] 每次手动操作写入 `action_logs`。
- [ ] 操作失败时返回统一错误响应，并记录内部错误。

### 1.7 前端 MVP 页面

- [ ] 实现账号配置页面。
- [ ] 实现实例配置页面。
- [ ] 实现实例列表页面。
- [ ] 展示当前 CDT 流量、阈值、实例状态、最近同步时间。
- [ ] 展示最近动作日志。
- [ ] 提供手动同步按钮。
- [ ] 提供手动启动、停止按钮，并显示确认和结果。
- [ ] 表单使用 React Hook Form + Zod。
- [ ] 服务端数据状态使用 TanStack Query。
- [ ] 所有 API 调用通过 `frontend/src/api/`。

### 1.8 MVP 验收

- [ ] 能配置一个阿里云账号。
- [ ] 能配置一个 ECS 实例。
- [ ] 能同步 CDT 流量。
- [ ] 能同步 ECS 状态。
- [ ] 超过阈值后能触发自动停机。
- [ ] 自动停机后有动作日志。
- [ ] 手动启动和停止都走后端 API。
- [ ] 前端能展示当前状态和最近日志。
- [ ] `go test ./...` 通过。
- [ ] 前端 typecheck/build 通过。

## 阶段 2：事件监听和秒级保活

目标：接收阿里云云监控事件 webhook，在允许保活时更快恢复实例。

### 2.1 Webhook 接入

- [ ] 设计 webhook token 生成和存储方式。
- [ ] 实现 `POST /api/webhooks/aliyun/events/:token`。
- [ ] 校验 token，拒绝未授权请求。
- [ ] 保存 webhook 原始事件必要字段。
- [ ] 解析事件 ID、实例 ID、事件时间、实例状态。
- [ ] 对无法解析的事件返回明确错误并记录日志。

### 2.2 事件幂等和存储

- [ ] 设计 `cloud_events` 表。
- [ ] 根据事件 ID、实例 ID、事件时间做幂等去重。
- [ ] 重复事件不重复触发启动。
- [ ] 记录事件处理状态和错误原因。

### 2.3 保活判断

- [ ] 收到 `Stopped` 事件后，调用 ECS API 二次确认实例状态。
- [ ] 判断实例是否启用保活。
- [ ] 判断是否因流量超限被系统停机。
- [ ] 判断是否因用户手动停机而阻止自动启动。
- [ ] 判断实例是否处于允许启动状态。
- [ ] 判断是否命中冷却时间。
- [ ] 符合条件时调用 ECS StartInstance。
- [ ] 记录保活启动动作日志。

### 2.4 兜底巡检

- [ ] 增加低频状态校准任务。
- [ ] webhook 丢失时，巡检仍能发现状态不一致。
- [ ] 巡检不能绕过流量超限停机标记。
- [ ] 巡检不能拉起用户手动停止的实例。

### 2.5 前端和文档

- [ ] 在实例设置中增加保活开关。
- [ ] 展示 webhook URL。
- [ ] 展示 webhook token 重置入口。
- [ ] 展示最近云事件记录。
- [ ] 补充阿里云云监控事件订阅配置说明。

### 2.6 阶段验收

- [ ] 后端能接收并记录阿里云事件 webhook。
- [ ] 重复事件不会重复启动实例。
- [ ] `Stopped` 事件在保活允许时能触发启动。
- [ ] 流量超限停机后不会被保活拉起。
- [ ] 用户手动停机后不会被保活拉起。
- [ ] webhook 丢失时低频巡检仍能校准状态。

## 阶段 3：配置和通知系统

目标：让关键动作和错误能及时通知用户。

### 3.1 系统设置

- [ ] 增加系统设置表。
- [ ] 支持全局同步频率配置。
- [ ] 支持默认阈值配置。
- [ ] 支持默认停机模式配置。
- [ ] 支持默认保活开关配置。
- [ ] 支持保活冷却时间配置。
- [ ] 前端实现系统设置页面。

### 3.2 通知通道

- [ ] 设计 `notification_channels` 表。
- [ ] 支持邮件通知配置。
- [ ] 支持 Telegram 通知配置。
- [ ] 支持通用 webhook 通知配置。
- [ ] 通知配置中的敏感字段不得明文展示。
- [ ] 实现通知测试接口。

### 3.3 通知事件

- [ ] 流量接近阈值时发送通知。
- [ ] 流量超限自动停机时发送通知。
- [ ] 收到实例停止事件时发送通知。
- [ ] 保活启动成功时发送通知。
- [ ] 保活启动失败时发送通知。
- [ ] 阿里云 API 鉴权失败时发送通知。
- [ ] 定时任务执行失败时发送通知。

### 3.4 通知日志

- [ ] 设计 `notification_logs` 表。
- [ ] 记录发送通道、事件类型、目标、结果、错误信息。
- [ ] 前端展示最近通知记录。
- [ ] 通知失败不能伪装成功。

### 3.5 阶段验收

- [ ] 用户能配置至少一种通知方式。
- [ ] 用户能发送测试通知。
- [ ] 自动停机能触发通知。
- [ ] 保活动作能触发通知。
- [ ] 通知失败有明确错误日志。

## 阶段 4：历史、图表和审计

目标：提升可观测性，方便排查和理解趋势。

### 4.1 历史数据

- [ ] 保存小时维度流量历史。
- [ ] 保存天维度流量历史。
- [ ] 保存实例状态变化历史。
- [ ] 保存动作日志的结构化字段。
- [ ] 保存 webhook 事件处理历史。

### 4.2 查询 API

- [ ] 实现流量趋势查询 API。
- [ ] 实现实例状态历史查询 API。
- [ ] 实现动作日志查询 API。
- [ ] 实现事件日志查询 API。
- [ ] 支持基础分页和时间范围过滤。

### 4.3 前端展示

- [ ] 实现流量趋势图。
- [ ] 在趋势图中展示阈值线。
- [ ] 实现实例状态历史视图。
- [ ] 实现动作日志列表。
- [ ] 实现事件日志列表。
- [ ] 区分同步、自动停机、手动操作、保活启动等动作来源。

### 4.4 日志清理

- [ ] 设计日志保留策略。
- [ ] 实现日志清理任务。
- [ ] 前端提供日志保留天数配置。
- [ ] 清理任务必须记录执行结果。

### 4.5 阶段验收

- [ ] 能查看最近流量趋势。
- [ ] 能查看实例状态变化历史。
- [ ] 能区分动作日志和事件日志。
- [ ] 日志可以按时间或类型清理。

## 阶段 5：增强功能

目标：在核心稳定后逐步补齐高级能力，每个功能独立闭环。

### 5.1 多账号、多区域、多实例

- [ ] 支持多个阿里云账号。
- [ ] 支持多个区域。
- [ ] 支持多个 ECS 实例。
- [ ] 同步任务按账号和区域分组执行。
- [ ] 前端支持按账号、区域、状态筛选实例。

### 5.2 定时开关机

- [ ] 设计定时任务表。
- [ ] 支持定时启动实例。
- [ ] 支持定时停止实例。
- [ ] 定时操作写入动作日志。
- [ ] 定时操作不得绕过流量超限保护。

### 5.3 月初自动恢复

- [ ] 支持月初重置流量保护状态。
- [ ] 支持月初自动启动配置了恢复策略的实例。
- [ ] 记录恢复动作和失败原因。

### 5.4 费用和余额查询

- [ ] 研究阿里云费用和余额 API。
- [ ] 设计费用快照表。
- [ ] 同步余额或费用风险指标。
- [ ] 前端展示费用状态。
- [ ] 费用 API 失败时不影响核心流量保护。

### 5.5 DDNS

- [ ] 支持 Cloudflare 凭据配置。
- [ ] 支持域名记录配置。
- [ ] 实例公网 IP 变化后更新 DNS。
- [ ] DDNS 操作记录日志。
- [ ] DDNS 失败不得影响流量保护。

### 5.6 高风险 ECS 操作

- [ ] 评估 ECS 创建功能。
- [ ] 评估 ECS 释放功能。
- [ ] 评估更换公网 IP 功能。
- [ ] 为高风险操作增加明确确认机制。
- [ ] 高风险操作必须记录审计日志。
- [ ] 高风险操作失败必须返回明确错误。

### 5.7 Telegram 远程控制

- [ ] 设计 Telegram Bot 配置。
- [ ] 支持查询实例状态。
- [ ] 支持触发手动启动。
- [ ] 支持触发手动停止。
- [ ] 高风险命令必须有确认或权限限制。
- [ ] Telegram 操作写入动作日志。

## 跨阶段质量任务

- [ ] 每次 API 变更同步更新前端 API client 和类型。
- [ ] 每次数据库变更验证空库初始化。
- [ ] 每次数据库变更验证旧库迁移路径。
- [ ] 每个云资源操作都必须记录动作日志。
- [ ] 每个外部 API 调用失败都必须有明确错误。
- [ ] 前端组件不得直接散落 `fetch`。
- [ ] 前端不得实现最终业务规则判断。
- [ ] 后端不得在 handler 中直接写 SQL。
- [ ] 不引入 Next.js、Redux、复杂状态管理、微服务、消息队列或 Kubernetes。
- [ ] 提交前检查没有密钥、Token、`.env` 或本地 SQLite 数据库进入 Git。

