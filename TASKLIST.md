# TASKLIST.md

## 使用说明

本文件把 `DEVELOPMENT_PLAN.md` 拆成可执行任务。开发时按阶段推进，每个任务应保持小步提交、可验证、可回滚。

任务状态约定：

- `[ ]` 未开始
- `[x]` 已完成

完成状态按可验收能力判断。仅完成数据模型、API 入口或 dry-run 链路时，应在任务文字中明确标注“原型”或“dry-run”，不得等同于真实外部服务已经可用。

## 当前生产化缺口

- [x] 接入真实阿里云 CDT 流量查询，并覆盖鉴权、限流和响应校验。
- [x] 接入真实阿里云 ECS 状态查询。
- [x] 接入真实 ECS 启动和停止操作，同时保留默认启用的 dry-run 模式和 read-only 模式。
- [ ] 接入真实 BssOpenApi 余额查询。
- [ ] 接入 Cloudflare DNS API，完成真实 DDNS 更新。
- [ ] 接入 Telegram Bot API 或 webhook。
- [ ] 接入邮件 SMTP 发送器。
- [x] 提供可复现的生产构建和 Docker Compose 部署方案。
- [ ] 完成真实云环境端到端验收。

阶段推进规则：

- 阶段 0 必须先完成，避免在空仓库上直接堆业务代码。
- 阶段 1 是核心 MVP，优先级最高。
- 阶段 2 的事件监听不能替代阶段 1 的周期巡检。
- 阶段 3 之后的功能必须建立在阶段 1 和阶段 2 稳定的基础上。

## 阶段 0：项目骨架

目标：建立可运行、可测试、可继续扩展的轻量全栈基础结构。

### 0.1 仓库基础

- [x] 确认 `reference/` 已加入 `.gitignore`，避免参考代码进入正式提交。
- [x] 保留 `AGENTS.md`、`DEVELOPMENT_PLAN.md`、`TASKLIST.md` 作为项目协作和规划文档。
- [x] 清理无业务价值的占位代码和临时 Docker 构建配置。
- [x] 明确本地开发命令，避免依赖隐式环境。

### 0.2 后端基础

- [x] 创建 `backend/` Go module。
- [x] 创建 `backend/cmd/server/` 启动入口。
- [x] 创建 `backend/internal/config/`，集中读取端口、数据库路径、日志级别等配置。
- [x] 创建 `backend/internal/http/`，提供路由、handler、统一 JSON 响应和错误响应。
- [x] 创建 `GET /healthz` 健康检查接口。
- [x] 创建 `GET /api/v1/status` 基础状态接口。
- [x] 实现管理员密码登录和 HttpOnly 会话 Cookie。
- [x] 默认保护 `/api/v1/*` 管理接口，未登录返回统一 401 JSON 错误。
- [x] 未配置 `CDTM_ADMIN_PASSWORD` 时后端拒绝启动。
- [x] 建立结构化日志，不输出密钥、Token、密码等敏感信息。
- [x] 所有请求链路使用 `context.Context`。

### 0.3 SQLite 基础

- [x] 创建 `backend/internal/store/`。
- [x] 接入 SQLite 连接管理。
- [x] 启用 `PRAGMA foreign_keys = ON`。
- [x] 创建简单 migration 机制。
- [x] 创建首个空库初始化流程。
- [x] 为 migration 增加执行记录表。
- [x] 验证空库启动时能自动完成初始化。

### 0.4 前端基础

- [x] 创建 `frontend/` React + Vite + TypeScript 项目。
- [x] 开启 TypeScript strict 风格。
- [x] 接入 Tailwind CSS。
- [x] 接入 shadcn/ui。
- [x] 建立 `frontend/src/api/` API client。
- [x] 建立 `frontend/src/components/` 通用组件目录。
- [x] 建立 `frontend/src/features/` 或 `frontend/src/pages/` 业务页面目录。
- [x] 使用 Vite 多 HTML 入口，拆分 `login.html`、`dashboard.html`、`settings.html`。
- [x] 前端能调用后端 `GET /healthz` 并展示基础状态。

### 0.5 本地开发和验证

- [x] 提供本地后端启动命令。
- [x] 提供本地前端启动命令。
- [x] 提供本地数据库目录配置。
- [x] 确认 `go test ./...` 可运行。
- [x] 确认前端 typecheck/build 命令可运行。
- [x] 补充最小 README，说明开发启动方式。

## 阶段 1：核心 MVP

目标：把 CDT 流量保护的最小业务闭环产品化。

### 1.1 数据模型和迁移

- [x] 设计 `aliyun_accounts` 表：AccessKey ID、加密后的 AccessKey Secret、区域、备注、启用状态、时间字段。
- [x] 设计 `ecs_instances` 表：账号 ID、实例 ID、名称、流量阈值、停机模式、启用状态、保活配置、最近状态、时间字段。
- [x] 设计 `traffic_snapshots` 表：账号 ID、统计周期、总流量、采集时间。
- [x] 设计 `action_logs` 表：动作类型、触发来源、目标实例、结果、错误信息、时间字段。
- [x] 为账号、实例、日志和采集时间添加必要索引。
- [x] 写入 migration，并验证空库初始化。

### 1.2 阿里云客户端

- [x] 定义阿里云 CDT 查询接口和 dry-run 实现。
- [x] 定义阿里云 ECS 查询接口和 dry-run 实现。
- [x] 定义 ECS 启动接口和 dry-run 实现。
- [x] 定义 ECS 停止接口和 dry-run 实现，支持 `StopCharging` 和 `KeepCharging`。
- [x] 使用阿里云官方 OpenAPI SDK 实现真实 CDT 和 ECS 查询。
- [x] 实现 `read-only` 模式，确保真实查询阶段禁止 ECS 变更。
- [x] 实现 `live` 模式的 ECS 启停，并向 StopInstance 传递停机模式。
- [x] 为阿里云 API 错误建立明确错误类型或错误码映射。
- [x] 确保日志和错误响应不泄露 AccessKey Secret。

### 1.3 配置管理 API

- [x] 实现账号创建 API。
- [x] 实现账号更新 API。
- [x] 实现账号列表 API。
- [x] 实现账号禁用或删除 API。
- [x] 实现实例创建 API。
- [x] 实现实例更新 API。
- [x] 实现实例列表 API。
- [x] 实现实例启用、禁用 API。
- [x] 后端对 AccessKey、区域、实例 ID、阈值、停机模式做最终校验。

### 1.4 流量和实例状态同步

- [x] 实现周期任务框架，支持可配置同步间隔。
- [x] 实现 CDT 流量同步任务。
- [x] 实现 ECS 实例状态同步任务。
- [x] 保存最近同步时间、同步结果和错误信息。
- [x] API 失败时记录日志，不伪造成功。
- [x] 增加手动同步接口，便于前端触发和排查。

### 1.5 阈值保护

- [x] 实现阈值判断 service。
- [x] 当 CDT 流量达到阈值时，自动停止启用保护的 ECS 实例。
- [x] 自动停机时记录触发原因、当时流量、阈值、停机模式和执行结果。
- [x] 流量超限自动停机后，设置状态标记，避免后续保活逻辑误启动。
- [x] 对停机中、已停止、启动中等状态做明确处理。
- [x] 增加 dry-run 或测试模式，便于验证判断逻辑而不操作真实 ECS。

### 1.6 手动操作

- [x] 实现手动启动 ECS API。
- [x] 实现手动停止 ECS API。
- [x] 手动停止时记录用户意图，避免保活逻辑自动拉起。
- [x] 每次手动操作写入 `action_logs`。
- [x] 操作失败时返回统一错误响应，并记录内部错误。

### 1.7 前端 MVP 页面

- [x] 实现账号配置页面。
- [x] 实现实例配置页面。
- [x] 实现实例列表页面。
- [x] 展示当前 CDT 流量、阈值、实例状态、最近同步时间。
- [x] 展示最近动作日志。
- [x] 提供手动同步按钮。
- [x] 提供手动启动、停止按钮，并显示确认和结果。
- [x] 表单使用 React Hook Form + Zod。
- [x] 服务端数据状态使用 TanStack Query。
- [x] 所有 API 调用通过 `frontend/src/api/`。

### 1.8 MVP 验收

- [x] 能配置一个阿里云账号。
- [x] 能配置一个 ECS 实例。
- [x] 能通过 dry-run 客户端验证 CDT 流量同步流程。
- [x] 能通过 dry-run 客户端验证 ECS 状态同步流程。
- [x] 能通过测试客户端验证超阈值自动停机流程。
- [ ] 能从真实阿里云同步 CDT 流量和 ECS 状态。
- [ ] 超过阈值后能在显式启用真实模式时停止目标 ECS。
- [x] 自动停机后有动作日志。
- [x] 手动启动和停止都走后端 API。
- [x] 前端能展示当前状态和最近日志。
- [x] `go test ./...` 通过。
- [x] 前端 typecheck/build 通过。

## 阶段 2：事件监听和秒级保活

目标：接收阿里云云监控事件 webhook，在允许保活时更快恢复实例。

### 2.1 Webhook 接入

- [x] 设计 webhook token 生成和存储方式。
- [x] 实现 `POST /api/webhooks/aliyun/events/:token`。
- [x] 校验 token，拒绝未授权请求。
- [x] 保存 webhook 原始事件必要字段。
- [x] 解析事件 ID、实例 ID、事件时间、实例状态。
- [x] 对无法解析的事件返回明确错误并记录日志。

### 2.2 事件幂等和存储

- [x] 设计 `cloud_events` 表。
- [x] 根据事件 ID、实例 ID、事件时间做幂等去重。
- [x] 重复事件不重复触发启动。
- [x] 记录事件处理状态和错误原因。

### 2.3 保活判断

- [x] 收到 `Stopped` 事件后，调用 ECS API 二次确认实例状态。
- [x] 判断实例是否启用保活。
- [x] 判断是否因流量超限被系统停机。
- [x] 判断是否因用户手动停机而阻止自动启动。
- [x] 判断实例是否处于允许启动状态。
- [x] 判断是否命中冷却时间。
- [x] 符合条件时调用 ECS StartInstance。
- [x] 记录保活启动动作日志。

### 2.4 兜底巡检

- [x] 增加低频状态校准任务。
- [x] webhook 丢失时，巡检仍能发现状态不一致。
- [x] 巡检不能绕过流量超限停机标记。
- [x] 巡检不能拉起用户手动停止的实例。

### 2.5 前端和文档

- [x] 在实例设置中增加保活开关。
- [x] 展示 webhook URL。
- [x] 展示 webhook token 重置入口。
- [x] 展示最近云事件记录。
- [x] 补充阿里云云监控事件订阅配置说明。

### 2.6 阶段验收

- [x] 后端能接收并记录阿里云事件 webhook。
- [x] 重复事件不会重复启动实例。
- [x] `Stopped` 事件在保活允许时能触发启动。
- [x] 流量超限停机后不会被保活拉起。
- [x] 用户手动停机后不会被保活拉起。
- [x] webhook 丢失时低频巡检仍能校准状态。

## 阶段 3：配置和通知系统

目标：让关键动作和错误能及时通知用户。

### 3.1 系统设置

- [x] 增加系统设置表。
- [x] 支持全局同步频率配置。
- [x] 支持默认阈值配置。
- [x] 支持默认停机模式配置。
- [x] 支持默认保活开关配置。
- [x] 支持保活冷却时间配置。
- [x] 前端实现系统设置页面。

### 3.2 通知通道

- [x] 设计 `notification_channels` 表。
- [x] 支持邮件通知配置模型。
- [x] 支持 Telegram 通知配置模型。
- [x] 支持通用 webhook 通知配置和发送。
- [ ] 实现邮件 SMTP 发送器。
- [ ] 实现 Telegram Bot API 发送器。
- [x] 通知配置中的敏感字段不得明文展示。
- [x] 实现通知测试接口。

### 3.3 通知事件

- [x] 流量接近阈值时发送通知。
- [x] 流量超限自动停机时发送通知。
- [x] 收到实例停止事件时发送通知。
- [x] 保活启动成功时发送通知。
- [x] 保活启动失败时发送通知。
- [x] 阿里云 API 鉴权失败时发送通知。
- [x] 定时任务执行失败时发送通知。

### 3.4 通知日志

- [x] 设计 `notification_logs` 表。
- [x] 记录发送通道、事件类型、目标、结果、错误信息。
- [x] 前端展示最近通知记录。
- [x] 通知失败不能伪装成功。

### 3.5 阶段验收

- [x] 用户能配置至少一种通知方式。
- [x] 用户能发送测试通知。
- [x] 自动停机能触发通知。
- [x] 保活动作能触发通知。
- [x] 通知失败有明确错误日志。

## 阶段 4：历史、图表和审计

目标：提升可观测性，方便排查和理解趋势。

### 4.1 历史数据

- [x] 保存小时维度流量历史。
- [x] 保存天维度流量历史。
- [x] 保存实例状态变化历史。
- [x] 保存动作日志的结构化字段。
- [x] 保存 webhook 事件处理历史。

### 4.2 查询 API

- [x] 实现流量趋势查询 API。
- [x] 实现实例状态历史查询 API。
- [x] 实现动作日志查询 API。
- [x] 实现事件日志查询 API。
- [x] 支持基础分页和时间范围过滤。

### 4.3 前端展示

- [x] 实现流量趋势图。
- [x] 在趋势图中展示阈值线。
- [x] 实现实例状态历史视图。
- [x] 实现动作日志列表。
- [x] 实现事件日志列表。
- [x] 区分同步、自动停机、手动操作、保活启动等动作来源。

### 4.4 日志清理

- [x] 设计日志保留策略。
- [x] 实现日志清理任务。
- [x] 前端提供日志保留天数配置。
- [x] 清理任务必须记录执行结果。

### 4.5 阶段验收

- [x] 能查看最近流量趋势。
- [x] 能查看实例状态变化历史。
- [x] 能区分动作日志和事件日志。
- [x] 日志可以按时间或类型清理。

## 阶段 5：增强功能

目标：在核心稳定后逐步补齐高级能力，每个功能独立闭环。

### 5.1 多账号、多区域、多实例

- [x] 支持多个阿里云账号。
- [x] 支持多个区域。
- [x] 支持多个 ECS 实例。
- [x] 同步任务按账号和区域分组执行。
- [x] 前端支持按账号、区域、状态筛选实例。

### 5.2 定时开关机

- [x] 设计定时任务表。
- [x] 支持定时启动实例。
- [x] 支持定时停止实例。
- [x] 定时操作写入动作日志。
- [x] 定时操作不得绕过流量超限保护。

### 5.3 月初自动恢复

- [x] 支持月初重置流量保护状态。
- [x] 支持月初自动启动配置了恢复策略的实例。
- [x] 记录恢复动作和失败原因。

### 5.4 费用和余额查询

- [x] 定义阿里云费用和余额接口边界。
- [x] 设计费用快照表。
- [x] 通过 dry-run 客户端验证费用同步流程。
- [ ] 接入真实 BssOpenApi 余额查询。
- [x] 前端展示费用状态。
- [x] 费用 API 失败时不影响核心流量保护。

### 5.5 DDNS

- [x] 支持 Cloudflare 凭据配置。
- [x] 支持域名记录配置。
- [x] 实现实例公网 IP 变化后的本地 DDNS 更新流程。
- [ ] 调用 Cloudflare DNS API 更新真实 DNS 记录。
- [x] DDNS 操作记录日志。
- [x] DDNS 失败不得影响流量保护。

### 5.6 高风险 ECS 操作

- [x] 评估 ECS 创建功能。
- [x] 评估 ECS 释放功能。
- [x] 评估更换公网 IP 功能。
- [x] 为高风险操作增加明确确认机制。
- [x] 高风险操作必须记录审计日志。
- [x] 高风险操作失败必须返回明确错误。

### 5.7 Telegram 远程控制

- [x] 设计 Telegram Bot 配置。
- [x] 实现查询实例状态的命令处理入口。
- [x] 实现手动启动的命令处理入口。
- [x] 实现手动停止的命令处理入口。
- [ ] 接入 Telegram webhook 或 polling，形成真实 Bot 入口。
- [x] 高风险命令必须有确认或权限限制。
- [x] Telegram 操作写入动作日志。

## 跨阶段质量任务

- [x] 每次 API 变更同步更新前端 API client 和类型。
- [x] 每次数据库变更验证空库初始化。
- [x] 每次数据库变更验证旧库迁移路径。
- [x] 每个云资源操作都必须记录动作日志。
- [x] 每个外部 API 调用失败都必须有明确错误。
- [x] 前端组件不得直接散落 `fetch`。
- [x] 管理页面必须先登录，不能直接开放配置修改入口。
- [x] 前端不能做成单一 React 路由入口，关键页面使用独立 HTML 入口。
- [x] 前端不得实现最终业务规则判断。
- [x] 后端不得在 handler 中直接写 SQL。
- [x] 不引入 Next.js、Redux、复杂状态管理、微服务、消息队列或 Kubernetes。
- [x] 提交前检查没有密钥、Token、`.env` 或本地 SQLite 数据库进入 Git。
